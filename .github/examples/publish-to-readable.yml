# Example: Publish a Markdown file to Readable from a GitHub Action.
#
# Setup:
#   1. Create an API key at https://readable.ashwinsathian.com → My Pages → Settings → API Keys
#   2. Add it as a repo secret: Settings → Secrets → Actions → READABLE_API_KEY
#   3. Optionally add READABLE_PAGE_ID as a repo variable to update the same page each run.
#
# Usage:
#   - Copy this file to .github/workflows/ in your own repo.
#   - Adjust the `file` input and trigger as needed.

name: Publish to Readable

on:
  release:
    types: [published]
  # Or trigger on push:
  # push:
  #   branches: [main]
  #   paths: [CHANGELOG.md]

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Publish to Readable
        id: publish
        env:
          READABLE_API_KEY: ${{ secrets.READABLE_API_KEY }}
        run: |
          if [ -n "${{ vars.READABLE_PAGE_ID }}" ]; then
            npx readable-cli publish CHANGELOG.md \
              --update ${{ vars.READABLE_PAGE_ID }}
          else
            npx readable-cli publish CHANGELOG.md \
              --slug release-notes \
              --visibility public
          fi
