{
  "name": "@readable/mcp-server",
  "version": "1.0.0",
  "description": "MCP server for publishing Markdown to Readable from AI assistants and agents",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "dev": "tsx src/node-server.ts",
    "serve:node": "node --import tsx/esm src/node-server.ts",
    "typecheck": "tsc --noEmit",
    "typecheck:node": "tsc --noEmit -p tsconfig.node.json"
  },
  "dependencies": {
    "readable-api-client": "^0.1.0"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "tsx": "^4.22.3",
    "typescript": "^5.0.0"
  }
}
