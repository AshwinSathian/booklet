{
  "name": "readable-vscode",
  "displayName": "Readable",
  "description": "Publish Markdown to a beautifully formatted, shareable Readable page — right from the editor",
  "version": "0.2.0",
  "publisher": "AshwinSathian",
  "icon": "images/icon.png",
  "license": "UNLICENSED",
  "homepage": "https://readable.ashwinsathian.com",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/AshwinSathian/readable.git",
    "directory": "packages/vscode"
  },
  "bugs": {
    "url": "https://github.com/AshwinSathian/readable/issues"
  },
  "author": {
    "name": "Ashwin Sathian",
    "email": "ashwinsathyan19@gmail.com"
  },
  "keywords": [
    "markdown",
    "publish",
    "share",
    "readable",
    "docs",
    "preview"
  ],
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "readable.publish",
        "title": "Readable: Publish Current File"
      },
      {
        "command": "readable.publishSelection",
        "title": "Readable: Publish Selection"
      },
      {
        "command": "readable.setApiKey",
        "title": "Readable: Set API Key"
      }
    ],
    "configuration": {
      "title": "Readable",
      "properties": {
        "readable.defaultVisibility": {
          "type": "string",
          "enum": [
            "public",
            "unlisted"
          ],
          "default": "unlisted",
          "description": "Default visibility for published pages"
        },
        "readable.baseUrl": {
          "type": "string",
          "default": "https://readable-api.ashwinsathian.com",
          "description": "Readable API base URL"
        }
      }
    }
  },
  "scripts": {
    "build": "tsup",
    "typecheck": "tsc --noEmit",
    "vscode:prepublish": "npm run build",
    "package": "vsce package --no-dependencies",
    "publish": "vsce publish --no-dependencies"
  },
  "dependencies": {
    "readable-api-client": "^0.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/vscode": "^1.85.0",
    "@vscode/vsce": "^3.9.2",
    "tsup": "^8.5.1",
    "typescript": "^5.0.0"
  }
}
